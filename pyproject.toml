[project]
name = "smart-data-cleaning"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "requests",
  "pydantic",
  "openenv-core"
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
entrypoint = "app:app"
